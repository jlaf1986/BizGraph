@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "UserAccount") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "UserAccount") ]
}
